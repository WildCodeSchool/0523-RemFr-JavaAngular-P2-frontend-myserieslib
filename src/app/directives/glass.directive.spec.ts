import { GlassDirective } from './glass.directive';

describe('GlassDirective', () => {
  it('should create an instance', () => {
    const directive = new GlassDirective();
    expect(directive).toBeTruthy();
  });
});
